#!/bin/bash
if [ -n "$1" ] && [ "$1" == "-task" ]; then
echo "Задание"
echo "36. Добавить запятые к числу с десятичной частью и знаком минуса."
echo
echo
fi
cat $1 | gsed -r ':a;s/(^|[^0-9.])([0-9]+)([0-9]{3})/\1\2,\3/g;ta'
exit 0